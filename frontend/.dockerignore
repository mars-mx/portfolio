node_modules
**/node_modules
.next
.git
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
# .env.local carries the local dev API URL — it must not leak into the image
# (Next would inline it at build time; the workflow passes the real value as
# a build arg instead).
.env*
.DS_Store
README.md
Dockerfile
.dockerignore
tsconfig.tsbuildinfo
